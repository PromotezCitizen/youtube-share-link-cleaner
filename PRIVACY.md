# Privacy Policy for YouTube Share Link Cleaner

Last updated: September 13, 2026

[English](#english) | [한국어](#한국어)

## English

YouTube Share Link Cleaner removes the `si` query parameter from YouTube share links, can shorten shared Shorts and live links, and copies clean links from regular video pages. This page describes what data it touches.

### Data handled by the extension

The extension reads the text YouTube puts in its share and copy fields, checks whether it is a YouTube URL with an `si` parameter, and rewrites it. **Copy clean link** reads the current page URL when pressed. All of this happens on the user's device. None of it is collected, kept, logged, or sent anywhere.

The extension stores two preferences on the user's device: whether `si` removal is enabled and whether shared Shorts and live links should be shortened. These are Boolean on/off values and do not contain personal information or browsing data.

### How data is used

YouTube share URLs are processed only to remove the `si` query parameter and, when the optional shortening setting is on, to convert `/shorts/VIDEO_ID` and `/live/VIDEO_ID` URLs to `youtu.be/VIDEO_ID`. The **Copy clean link** button converts the current `youtube.com/watch?v=VIDEO_ID` URL to `youtu.be/VIDEO_ID` and removes `si`. Other URL information, including timestamps and playlist parameters, is preserved.

The two stored settings only remember what the user switched on or off.

### Storage and retention

Rewritten URLs are not kept.

The on/off preferences remain in Chrome's local extension storage until the user changes it, clears the extension's data, or uninstalls the extension. Chrome removes the extension's local storage when the extension is uninstalled.

### Data transmission and sharing

Nothing is sent to the developer, to servers, or to third parties. No data is sold or shared.

### Third-party services

The extension does not use analytics, advertising, tracking, authentication, or other third-party services.

### User controls

Users can turn `si` removal and Shorts/live link shortening on or off from the extension popup. They can delete the stored preferences by clearing the extension's data in Chrome or uninstalling the extension.

### Limited use

Data is used only to clean and copy YouTube links as described above. It is not used for advertising, profiling, creditworthiness, lending, or anything unrelated, and no person reviews it.

### Changes to this policy

If the extension changes what it does with data, this page changes with it. The revision date is at the top.

### Contact

For privacy questions, bug reports, or support, open an issue at [GitHub Issues](https://github.com/PromotezCitizen/youtube-share-link-cleaner/issues). Do not include private or sensitive information in a public issue.

This extension is not affiliated with or endorsed by YouTube or Google.

---

## 한국어

YouTube 공유 링크 정리(YouTube Share Link Cleaner)는 YouTube 공유 링크에서 `si` 쿼리 파라미터를 빼고, 원하면 Shorts·라이브 링크를 줄이며, 일반 동영상 페이지에서 정리된 링크를 복사하는 확장 프로그램입니다. 이 문서는 어떤 데이터를 다루는지 적은 것입니다.

### 확장 프로그램이 처리하는 데이터

확장 프로그램은 YouTube가 공유·복사 칸에 넣는 텍스트를 읽어 `si` 파라미터가 붙은 YouTube URL인지 확인하고 고쳐 씁니다. **정리해 복사**는 버튼을 눌렀을 때 현재 페이지 URL을 읽습니다. 모두 사용자 기기 내에서 일어나며, 수집·보관·기록·전송하지 않습니다.

확장 프로그램은 두 가지 설정만 사용자의 기기에 저장합니다. `si` 제거의 켜짐/꺼짐 여부와 Shorts·라이브 링크 줄이기의 켜짐/꺼짐 여부입니다. 두 설정 모두 참/거짓 값이며 개인정보나 방문 정보를 포함하지 않습니다.

### 데이터 이용 목적

YouTube 공유 URL은 `si` 쿼리 파라미터를 제거하고, 줄이기 설정이 켜져 있으면 `/shorts/VIDEO_ID`와 `/live/VIDEO_ID` URL을 `youtu.be/VIDEO_ID`로 바꾸는 목적으로만 처리됩니다. **정리해 복사** 버튼은 현재 `youtube.com/watch?v=VIDEO_ID` URL을 `youtu.be/VIDEO_ID`로 바꾸고 `si`를 제거합니다. 시작 시간과 재생목록을 포함한 나머지 URL 정보는 유지됩니다.

저장된 두 설정은 사용자가 무엇을 켜고 껐는지 기억하는 데만 씁니다.

### 저장 및 보관 기간

고쳐 쓴 URL은 보관하지 않습니다.

켜짐/꺼짐 설정은 사용자가 설정을 변경하거나 확장 프로그램 데이터를 삭제하거나 확장 프로그램을 제거할 때까지 Chrome의 로컬 확장 프로그램 저장소에 보관됩니다. 확장 프로그램을 제거하면 Chrome이 해당 로컬 저장소도 삭제합니다.

### 외부 전송 및 제3자 제공

개발자, 서버, 제3자 어디에도 데이터를 보내지 않습니다. 팔거나 공유하지도 않습니다.

### 외부 서비스

확장 프로그램은 분석, 광고, 추적, 인증 또는 그 밖의 제3자 서비스를 사용하지 않습니다.

### 사용자 제어

사용자는 확장 프로그램 팝업에서 `si` 제거와 Shorts·라이브 줄이기를 각각 켜거나 끌 수 있습니다. Chrome에서 확장 프로그램 데이터를 삭제하거나 확장 프로그램을 제거하여 저장된 설정을 삭제할 수 있습니다.

### 제한적 사용

데이터는 위에 적은 대로 YouTube 링크를 정리하고 복사하는 데만 씁니다. 광고, 프로파일링, 신용도 평가, 대출이나 그 밖의 관련 없는 목적에 쓰지 않으며, 사람이 열람하지 않습니다.

### 개인정보 처리방침 변경

확장 프로그램이 데이터를 다루는 방식이 바뀌면 이 문서도 함께 바꿉니다. 수정일은 문서 맨 위에 있습니다.

### 문의

개인정보 관련 질문, 오류 제보 및 지원 요청은 [GitHub Issues](https://github.com/PromotezCitizen/youtube-share-link-cleaner/issues)에 남겨 주세요. 공개 이슈에 개인정보나 민감한 정보를 포함하지 마세요.

이 확장 프로그램은 YouTube 또는 Google과 제휴 관계가 없으며 이들의 보증을 받지 않았습니다.
